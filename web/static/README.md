# Static assets for Saint Hubbins WASM build
# `web/static/saint-hubbins.wasm` is generated via:
# GOOS=js GOARCH=wasm go build -o web/static/saint-hubbins.wasm ./cmd/saint-hubbins-wasm
# and `wasm_exec.js` is copied from `$(go env GOROOT)/misc/wasm/wasm_exec.js`
